1.  var ar  = new array(100);
2. for(var i=0, len=ar.length; i<len; i++)
{
    if(ar[i]%2==0)
    {
        console.log(ar[i])
    }
}
3.
arr = ar.forEach((el) => (el%2 == 0)?e1*2:"")

4.
 var sum = ar.reduce(function(a, b){
        return a + b;
    }, 0);
    console.log(sum);

5.
    